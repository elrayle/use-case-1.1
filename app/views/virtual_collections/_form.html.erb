<%= form_for(@virtual_collection) do |f| %>
  <% if @virtual_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@virtual_collection.errors.count, "error") %> prohibited this virtual_collection from being saved:</h2>

      <ul>
      <% @virtual_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
